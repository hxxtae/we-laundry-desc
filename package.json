{
  "name": "we-laundry-desc",
  "version": "2.0.0",
  "description": "description website of welaundry",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rm_dev": "rm -rf dist/dev",
    "rm_build": "rm -rf dist/build",
    "dev": "npm run rm_dev && parcel index.html -d dist/dev",
    "build": "npm run rm_build && parcel build index.html --public-url ././ -d dist/build",
    "deploy": "gh-pages -d dist/build",
    "sass": "node-sass -wr --source-map true ./src/styles/main.scss ./style.css"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.3.0",
    "gh-pages": "^4.0.0",
    "node-sass": "^7.0.1",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.5",
    "aos": "^2.3.4",
    "bootstrap": "^5.2.0"
  },
  "homepage": "https://hxxtae.github.io/we-laundry-desc"
}
